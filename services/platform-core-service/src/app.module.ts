import{BadRequestException,Body,Controller,Delete,Get,Headers,Module,Param,Patch,Post,Query}from'@nestjs/common';import{z}from'zod';import{Db}from'./db.js';import{AccessService,verifyUserJwt}from'./security.js';import{AuthAdminService}from'./auth-admin.js';
const uuid=z.string().uuid();const parse=<T>(s:z.ZodType<T>,v:unknown)=>{const r=s.safeParse(v);if(!r.success)throw new BadRequestException(r.error.flatten());return r.data};const auditSchema=z.object({agencyId:uuid,clientId:uuid.nullable().optional(),eventType:z.string().trim().min(1).max(80),action:z.string().trim().min(1).max(120),entityType:z.string().trim().min(1).max(80),entityId:z.string().trim().max(160).nullable().optional(),metadata:z.record(z.string(),z.unknown()).default({})});
const profileSchema=z.object({agencyId:uuid,name:z.string().trim().min(1).max(160),legalName:z.string().max(200).nullable().optional(),contactEmail:z.string().email().nullable().optional(),phone:z.string().max(40).nullable().optional(),website:z.string().url().nullable().optional(),country:z.string().max(80).nullable().optional(),language:z.string().max(12).default('ru'),timezone:z.string().min(1),currency:z.string().max(8),address:z.record(z.string(),z.unknown()).default({}),registrationNumber:z.string().max(120).nullable().optional(),taxId:z.string().max(120).nullable().optional(),workingHours:z.record(z.string(),z.unknown()).default({}),agencyMarkup:z.number().min(0).max(10000).default(0),defaultSettings:z.record(z.string(),z.unknown()).default({}),branding:z.record(z.string(),z.unknown()).default({})});
const inviteSchema=z.object({agencyId:uuid,email:z.string().email(),role:z.enum(['admin','staff','client']).default('staff'),permissions:z.array(z.string()).default([]),redirectTo:z.string().url().optional()});const memberSchema=z.object({agencyId:uuid,role:z.enum(['admin','staff','client']),status:z.enum(['invited','active','suspended']),permissions:z.array(z.string()).default([])});const teamSchema=z.object({agencyId:uuid,name:z.string().trim().min(1).max(120),description:z.string().max(2000).default(''),color:z.string().regex(/^#[0-9A-Fa-f]{6}$/).default('#64748B'),memberIds:z.array(uuid).default([]),clientIds:z.array(uuid).default([])});const subscriptionSchema=z.object({agencyId:uuid,plan:z.enum(['trial','starter','growth','scale']),billingCycle:z.enum(['monthly','yearly']).default('monthly'),couponCode:z.string().max(80).nullable().optional()});const onboardingSchema=z.object({agencyId:uuid,step:z.enum(['welcome','company','branding','firstClient','firstIntegration','firstDashboard','firstReport']),complete:z.boolean()});
@Controller('health')class HealthController{@Get()health(){return{ok:true,service:'platform-core-service',modules:['tenants','agency-management','users-teams','billing','onboarding','permissions','audit']}}}
@Controller('v1/platform')class PlatformController{constructor(private db:Db,private access:AccessService,private auth:AuthAdminService){}private async audit(agencyId:string,userId:string,eventType:string,action:string,entityType:string,entityId:string|null,metadata:Record<string,unknown>={}){await this.db.query(`insert into public.audit_logs(agency_id,user_id,event_type,action,entity_type,entity_id,metadata) values($1,$2,$3,$4,$5,$6,$7)`,[agencyId,userId,eventType,action,entityType,entityId,metadata])}
@Get('tenants')async tenants(@Headers('authorization')a:string){const u=await verifyUserJwt(a);const r=await this.db.query(`select a.id,a.name,a.slug,a.plan,a.trial_ends_at,a.branding,m.role::text,m.permissions,m.status from public.agency_memberships m join public.agencies a on a.id=m.agency_id where m.user_id=$1 and m.status='active' order by a.name`,[u.userId]);return{items:r.rows}}
@Get('workspace')async workspace(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);parse(uuid,agencyId);const membership=await this.access.membership(u.userId,agencyId);const[agency,clients,plan,override,flags,profile]=await Promise.all([this.db.query(`select * from public.agencies where id=$1`,[agencyId]),this.db.query(`select c.id,c.company,c.url,c.timezone,c.country,c.language,c.status,c.logo_url,c.brand_color,c.updated_at from public.clients c where c.agency_id=$1 and c.status<>'archived' order by c.company`,[agencyId]),this.db.query(`select pe.entitlements,pe.limits from public.agencies a left join public.plan_entitlements pe on pe.plan=a.plan where a.id=$1`,[agencyId]),this.db.query(`select entitlements,limits from public.agency_entitlement_overrides where agency_id=$1`,[agencyId]),this.db.query(`select f.key,coalesce(af.enabled,f.default_enabled) enabled,coalesce(af.config,f.metadata) config from public.feature_flags f left join public.agency_feature_flags af on af.flag_key=f.key and af.agency_id=$1 order by f.key`,[agencyId]),this.db.query(`select p.user_id,p.name,p.avatar_url,p.locale,u.email from public.user_profiles p left join auth.users u on u.id=p.user_id where p.user_id=$1`,[u.userId])]);if(!agency.rows[0])throw new BadRequestException('AGENCY_NOT_FOUND');const base=plan.rows[0]??{entitlements:{},limits:{}};const custom=override.rows[0]??{entitlements:{},limits:{}};return{currentUser:profile.rows[0]??{user_id:u.userId,email:u.email},agency:agency.rows[0],membership,clients:clients.rows,permissions:membership.permissions,entitlements:{entitlements:{...(base.entitlements??{}),...(custom.entitlements??{})},limits:{...(base.limits??{}),...(custom.limits??{})}},featureFlags:Object.fromEntries(flags.rows.map((x:any)=>[x.key,{enabled:x.enabled,config:x.config}]))}}
@Get('agency')async agency(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'agency.read');const r=await this.db.query(`select * from public.agencies where id=$1`,[agencyId]);return r.rows[0]}
@Patch('agency')async updateAgency(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(profileSchema,body);await this.access.require(u.userId,x.agencyId,'agency.manage');const r=await this.db.query(`update public.agencies set name=$2,legal_name=$3,contact_email=$4,phone=$5,website=$6,country=$7,language=$8,timezone=$9,currency=$10,address=$11,registration_number=$12,tax_id=$13,working_hours=$14,agency_markup=$15,default_settings=$16,branding=branding||$17::jsonb,updated_at=now() where id=$1 returning *`,[x.agencyId,x.name,x.legalName??null,x.contactEmail??null,x.phone??null,x.website??null,x.country??null,x.language,x.timezone,x.currency,x.address,x.registrationNumber??null,x.taxId??null,x.workingHours,x.agencyMarkup,x.defaultSettings,x.branding]);await this.audit(x.agencyId,u.userId,'agency.updated','update','agency',x.agencyId,{fields:Object.keys(x)});return r.rows[0]}
@Get('memberships')async memberships(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'users.read');const r=await this.db.query(`select m.id,m.user_id,m.role::text,m.permissions,m.status,m.created_at,m.updated_at,p.name,p.avatar_url,u.email from public.agency_memberships m left join public.user_profiles p on p.user_id=m.user_id left join auth.users u on u.id=m.user_id where m.agency_id=$1 order by p.name nulls last,u.email`,[agencyId]);return{items:r.rows}}
@Post('memberships/invite')async invite(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(inviteSchema,body);await this.access.require(u.userId,x.agencyId,'users.manage');const existing=await this.auth.find(x.email);const user=existing??await this.auth.invite(x.email,x.redirectTo);const r=await this.db.query(`insert into public.agency_memberships(agency_id,user_id,role,permissions,status) values($1,$2,$3,$4,$5) on conflict(agency_id,user_id) do update set role=excluded.role,permissions=excluded.permissions,status=excluded.status,updated_at=now() returning *`,[x.agencyId,user.id,x.role,x.permissions,existing?'active':'invited']);await this.audit(x.agencyId,u.userId,'agency_member.invited','invite','user',user.id,{email:x.email,role:x.role});return r.rows[0]}
@Patch('memberships/:userId')async updateMember(@Headers('authorization')a:string,@Param('userId')userId:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(memberSchema,body);await this.access.require(u.userId,x.agencyId,'users.manage');if(userId===u.userId&&x.status!=='active')throw new BadRequestException('CANNOT_DEACTIVATE_SELF');const r=await this.db.query(`update public.agency_memberships set role=$3,status=$4,permissions=$5,updated_at=now() where agency_id=$1 and user_id=$2 returning *`,[x.agencyId,parse(uuid,userId),x.role,x.status,x.permissions]);await this.audit(x.agencyId,u.userId,'agency_member.updated','update','user',userId,{role:x.role,status:x.status});return r.rows[0]}
@Get('teams')async teams(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'users.read');const r=await this.db.query(`select t.*,coalesce((select jsonb_agg(user_id) from public.agency_team_members tm where tm.team_id=t.id),'[]') member_ids,coalesce((select jsonb_agg(client_id) from public.agency_team_clients tc where tc.team_id=t.id),'[]') client_ids from public.agency_teams t where t.agency_id=$1 order by t.name`,[agencyId]);return{items:r.rows}}
@Post('teams')async createTeam(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(teamSchema,body);await this.access.require(u.userId,x.agencyId,'users.manage');const r=await this.db.query(`insert into public.agency_teams(agency_id,name,description,color,created_by) values($1,$2,$3,$4,$5) returning *`,[x.agencyId,x.name,x.description,x.color,u.userId]);await this.replaceTeamLinks(r.rows[0].id,x);await this.audit(x.agencyId,u.userId,'agency_team.created','create','team',r.rows[0].id,{name:x.name});return r.rows[0]}
@Patch('teams/:id')async updateTeam(@Headers('authorization')a:string,@Param('id')id:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(teamSchema,body);await this.access.require(u.userId,x.agencyId,'users.manage');const r=await this.db.query(`update public.agency_teams set name=$3,description=$4,color=$5,updated_at=now() where id=$1 and agency_id=$2 returning *`,[parse(uuid,id),x.agencyId,x.name,x.description,x.color]);await this.replaceTeamLinks(id,x);await this.audit(x.agencyId,u.userId,'agency_team.updated','update','team',id,{name:x.name});return r.rows[0]}
@Delete('teams/:id')async deleteTeam(@Headers('authorization')a:string,@Param('id')id:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'users.manage');await this.db.query(`delete from public.agency_teams where id=$1 and agency_id=$2`,[parse(uuid,id),agencyId]);await this.audit(agencyId,u.userId,'agency_team.deleted','delete','team',id);return{ok:true}}
private async replaceTeamLinks(id:string,x:z.infer<typeof teamSchema>){await this.db.query(`delete from public.agency_team_members where team_id=$1`,[id]);await this.db.query(`delete from public.agency_team_clients where team_id=$1`,[id]);for(const userId of x.memberIds)await this.db.query(`insert into public.agency_team_members(team_id,agency_id,user_id) values($1,$2,$3)`,[id,x.agencyId,userId]);for(const clientId of x.clientIds)await this.db.query(`insert into public.agency_team_clients(team_id,agency_id,client_id) select $1,$2,$3 where exists(select 1 from public.clients where id=$3 and agency_id=$2)`,[id,x.agencyId,clientId])}
@Get('billing')async billing(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'billing.read');const[subscription,usage,invoices,plans]=await Promise.all([this.db.query(`select s.*,p.entitlements,p.limits from public.agency_subscriptions s join public.plan_entitlements p on p.plan=s.plan where s.agency_id=$1`,[agencyId]),this.db.query(`select (select count(*)::int from public.clients where agency_id=$1 and status<>'archived') clients,(select count(*)::int from public.agency_memberships where agency_id=$1 and status='active') users,(select count(*)::int from public.data_sources where agency_id=$1 and status<>'disconnected') integrations`,[agencyId]),this.db.query(`select * from public.agency_invoices where agency_id=$1 order by issued_at desc limit 50`,[agencyId]),this.db.query(`select plan,entitlements,limits from public.plan_entitlements order by case plan when 'trial' then 1 when 'starter' then 2 when 'growth' then 3 else 4 end`)]);return{subscription:subscription.rows[0],usage:usage.rows[0],invoices:invoices.rows,plans:plans.rows}}
@Patch('billing/subscription')async subscription(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(subscriptionSchema,body);await this.access.require(u.userId,x.agencyId,'billing.manage');const r=await this.db.query(`update public.agency_subscriptions set plan=$2,status=case when $2='trial' then 'trialing' else 'active' end,billing_cycle=$3,coupon_code=$4,cancel_at_period_end=false,cancelled_at=null,current_period_start=now(),current_period_end=now()+case when $3='yearly' then interval '1 year' else interval '1 month' end where agency_id=$1 returning *`,[x.agencyId,x.plan,x.billingCycle,x.couponCode??null]);await this.db.query(`update public.agencies set plan=$2,updated_at=now() where id=$1`,[x.agencyId,x.plan]);await this.audit(x.agencyId,u.userId,'subscription.updated','update','subscription',x.agencyId,{plan:x.plan,billingCycle:x.billingCycle});return r.rows[0]}
@Post('billing/cancel')async cancel(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(z.object({agencyId:uuid}),body);await this.access.require(u.userId,x.agencyId,'billing.manage');const r=await this.db.query(`update public.agency_subscriptions set cancel_at_period_end=true,cancelled_at=now(),updated_at=now() where agency_id=$1 returning *`,[x.agencyId]);await this.audit(x.agencyId,u.userId,'subscription.cancelled','cancel','subscription',x.agencyId);return r.rows[0]}
@Get('onboarding')async onboarding(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'onboarding.read');const r=await this.db.query(`select * from public.agency_onboarding_progress where agency_id=$1`,[agencyId]);return r.rows[0]}
@Patch('onboarding')async updateOnboarding(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(onboardingSchema,body);await this.access.require(u.userId,x.agencyId,'onboarding.manage');const r=await this.db.query(`update public.agency_onboarding_progress set steps=jsonb_set(steps,array[$2],to_jsonb($3::boolean),true),current_step=$2,completed_at=case when (steps||jsonb_build_object($2,$3)) @> '{"welcome":true,"company":true,"branding":true,"firstClient":true,"firstIntegration":true,"firstDashboard":true,"firstReport":true}'::jsonb then now() else null end,updated_by=$4,updated_at=now() where agency_id=$1 returning *`,[x.agencyId,x.step,x.complete,u.userId]);if(r.rows[0]?.completed_at)await this.db.query(`update public.agencies set onboarding_completed_at=$2 where id=$1`,[x.agencyId,r.rows[0].completed_at]);await this.audit(x.agencyId,u.userId,'onboarding.updated','update','agency',x.agencyId,{step:x.step,complete:x.complete});return r.rows[0]}
@Get('permissions')async permissions(@Headers('authorization')a:string,@Query('agencyId')agencyId:string){const u=await verifyUserJwt(a);const m=await this.access.require(u.userId,parse(uuid,agencyId),'permissions.read');const r=await this.db.query(`select key,module,description,risk_level from public.permission_registry order by module,key`);return{role:m.role,granted:m.permissions,registry:r.rows}}
@Get('audit')async auditList(@Headers('authorization')a:string,@Query('agencyId')agencyId:string,@Query('limit')limit='100'){const u=await verifyUserJwt(a);await this.access.require(u.userId,parse(uuid,agencyId),'audit.read');const r=await this.db.query(`select * from public.audit_logs where agency_id=$1 order by created_at desc limit $2`,[agencyId,Math.min(500,Math.max(1,Number(limit)||100))]);return{items:r.rows}}
@Post('audit')async recordAudit(@Headers('authorization')a:string,@Body()body:unknown){const u=await verifyUserJwt(a);const x=parse(auditSchema,body);await this.access.membership(u.userId,x.agencyId);await this.audit(x.agencyId,u.userId,x.eventType,x.action,x.entityType,x.entityId??null,x.metadata);return{ok:true}}
}
@Module({controllers:[HealthController,PlatformController],providers:[Db,AccessService,AuthAdminService]})export class AppModule{}
