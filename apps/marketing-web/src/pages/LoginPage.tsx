import {FormEvent,useState} from 'react';
import {Button,Card} from '@imds/ui';
import {useAuth} from '../app/AuthProvider';
export function LoginPage(){const{signIn,error,loading}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');async function submit(event:FormEvent){event.preventDefault();await signIn(email,password)}return <div className="auth-page"><Card className="auth-card"><div className="brand auth-brand"><div className="brand-mark">IM</div><div><strong>IMDS</strong><span>Marketing Platform</span></div></div><h1>Вход в Marketing Platform</h1><p>Используйте учётную запись вашей организации.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Пароль<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<div className="form-error">{error}</div>}<Button disabled={loading}>{loading?'Входим…':'Войти'}</Button></form></Card></div>}
