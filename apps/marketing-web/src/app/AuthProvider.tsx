import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from 'react';
import type {Session} from '@supabase/supabase-js';
import type {WorkspaceBootstrap} from '@imds/api-client';
import {getCurrentSession,hasSupabaseEnvironment,loadWorkspaceBootstrap,onAuthStateChange,signInWithPassword,signOut} from '@imds/auth';
type AuthState={configured:boolean;loading:boolean;session:Session|null;workspace:WorkspaceBootstrap|null;error:string|null;signIn:(email:string,password:string)=>Promise<void>;signOut:()=>Promise<void>;switchAgency:(agencyId:string)=>Promise<void>;refresh:()=>Promise<void>};
const AuthContext=createContext<AuthState|null>(null);
export function AuthProvider({children}:{children:ReactNode}){const configured=hasSupabaseEnvironment();const[loading,setLoading]=useState(true);const[session,setSession]=useState<Session|null>(null);const[workspace,setWorkspace]=useState<WorkspaceBootstrap|null>(null);const[error,setError]=useState<string|null>(null);const hydrate=async(activeAgencyId?:string|null)=>{setLoading(true);setError(null);try{const nextSession=await getCurrentSession();setSession(nextSession);if(configured&&!nextSession){setWorkspace(null);return}setWorkspace(await loadWorkspaceBootstrap(activeAgencyId));}catch(value){setError(value instanceof Error?value.message:'Could not load workspace');setWorkspace(null)}finally{setLoading(false)}};useEffect(()=>{void hydrate();return onAuthStateChange(next=>{setSession(next);void hydrate()})},[]);const value=useMemo<AuthState>(()=>({configured,loading,session,workspace,error,signIn:async(email,password)=>{setLoading(true);setError(null);try{await signInWithPassword(email,password);await hydrate()}catch(value){setError(value instanceof Error?value.message:'Sign in failed');setLoading(false);throw value}},signOut:async()=>{await signOut();setSession(null);setWorkspace(null)},switchAgency:async agencyId=>hydrate(agencyId),refresh:()=>hydrate(workspace?.activeAgency?.id)}),[configured,loading,session,workspace,error]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export function useAuth(){const value=useContext(AuthContext);if(!value)throw new Error('useAuth must be used inside AuthProvider');return value}
