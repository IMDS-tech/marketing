import {getSupabaseBrowserClient} from '@imds/auth';
const base=(import.meta.env.VITE_INTEGRATION_SERVICE_URL||'').replace(/\/+$/,'');
async function request(path:string,options:RequestInit={}){if(!base)throw new Error('Integration Service URL is not configured');const {data}=await getSupabaseBrowserClient().auth.getSession();const token=data.session?.access_token;if(!token)throw new Error('Authentication required');const response=await fetch(`${base}${path}`,{...options,headers:{'content-type':'application/json',authorization:`Bearer ${token}`,...options.headers}});const payload=await response.json().catch(()=>null);if(!response.ok)throw new Error(payload?.message||`Integration Service HTTP ${response.status}`);return payload}
export const integrationApi={start:(provider:string,body:unknown)=>request(`/v1/oauth/${provider}/start`,{method:'POST',body:JSON.stringify(body)}),accounts:(handle:string)=>request(`/v1/oauth/connections/${handle}/accounts`),attach:(body:unknown)=>request('/v1/oauth/connections/attach',{method:'POST',body:JSON.stringify(body)})};
