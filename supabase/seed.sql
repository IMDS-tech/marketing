-- Tenant demo data is created by `pnpm bootstrap:local` through the Auth Admin API.
-- Reference catalogs and plan entitlements are seeded by versioned migrations.
-- Keep this file data-only and safe to run after every local database reset.
select 'IMDS Marketing seed ready' as seed_status;
