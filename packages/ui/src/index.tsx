import type { ButtonHTMLAttributes, HTMLAttributes, ReactNode } from 'react';
export function Button({className='',variant='primary',...props}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'}){return <button className={`ui-button ui-button--${variant} ${className}`.trim()} {...props}/>}
export function Badge({children,tone='neutral'}:{children:ReactNode;tone?:'neutral'|'success'|'warning'|'danger'|'info'}){return <span className={`ui-badge ui-badge--${tone}`}>{children}</span>}
export function Card({className='',...props}:HTMLAttributes<HTMLDivElement>){return <div className={`ui-card ${className}`.trim()} {...props}/>}
export function EmptyState({icon,title,description,benefits,action}:{icon:ReactNode;title:string;description:string;benefits:string[];action:ReactNode}){return <Card className="empty-state"><div><div className="empty-state__icon">{icon}</div><h2>{title}</h2><p>{description}</p><ul>{benefits.map(item=><li key={item}>{item}</li>)}</ul>{action}</div><div className="empty-state__preview" aria-hidden="true"><span className="preview-line preview-line--wide"/><span className="preview-line"/><div className="preview-grid"><span/><span/><span/></div></div></Card>}
