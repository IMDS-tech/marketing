# Phase 1 — Application skeleton

Implemented scope:

- agency/client tenancy schema and RLS;
- workspace bootstrap RPC;
- React application shell;
- sidebar information architecture;
- Clients page with demo fallback;
- shared packages: ui, api-client, auth, permissions, integrations, analytics;
- Cloudflare static assets build from `apps/marketing-web/dist`.

The migration is committed but must be applied only after selecting the intended Supabase project and reviewing advisors.
